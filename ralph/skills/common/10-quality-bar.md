## Quality bar

- Avoid breaking existing behavior.
- Keep scripts portable: bash strict mode, quote variables, handle missing commands.
- Make failure modes explicit and readable.
- Don’t introduce hidden magic; keep configs as files in the repo.